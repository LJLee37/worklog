# CLAUDE.md - worklog

> ⚠️ **이 저장소는 GitHub public 저장소다. 여기에 커밋되는 모든 내용은
> 전세계에 공개된다.** 이 지침은 이 저장소를 여는 모든 Claude Code(혹은
> 다른 AI 코딩 도구) 세션에 적용된다 — 어느 기기에서 열었든 예외 없음.

## 이 저장소의 목적

개인 작업 기록(코딩/서버 설정 등)과, Claude Code 등 AI를 활용한 작업 기록을
공개로 남기는 저장소다. 구조는 `<YYYY>/<YYYY-MM-DD>.md` — 하루에 파일 하나.

## 절대 규칙: 민감정보를 커밋하지 않는다

아래에 해당하는 내용은 **어떤 이유로도 이 저장소에 커밋하지 않는다.** 요약해서
"무엇을 했는지"만 남기고, 구체적인 값은 절대 옮기지 않는다:

- 비밀번호, API 키, 토큰, 액세스 키(AWS/Cloudflare 등)
- SSH 개인키(공개키도 신중히 — 굳이 필요하지 않으면 넣지 않는다)
- VPN/EAP 자격증명, ansible-vault 평문 내용
- 실제 도메인/공인 IP 등 이 인프라를 특정할 수 있는 구체 정보
- 다른 사람의 개인정보(이름, 연락처 등)

값이 꼭 필요해 보이면 `<REDACTED>`로 표시하거나, 값 대신 "무엇을 했는지"를
서술하는 문장으로 대체한다 (예: "VPN EAP 자격증명을 vault.yml에 추가함" ✅,
실제 identity/password 값 ❌).

## 커밋 전 체크리스트 (매번)

1. `git diff --cached`로 스테이징된 내용을 실제로 읽어본다 — 자동으로 넘어가지
   않는다.
2. 위 금지 목록에 해당하는 내용이 없는지 확인한다.
3. `githooks/pre-commit` 훅이 활성화돼 있어야 한다(`git config core.hooksPath
   githooks`). 이 훅은 흔한 시크릿 패턴을 잡아내는 **마지막 안전망**일 뿐,
   이 지침(1차 방어선)을 대체하지 않는다 — 훅이 통과했다고 안전하다는 뜻은
   아니다.
4. 새로 이 저장소를 clone한 기기에서는 `core.hooksPath`가 로컬 설정이라
   자동으로 따라오지 않으므로, 첫 작업 전에 `git config core.hooksPath
   githooks`를 반드시 실행한다.

## `/log` 슬래시 명령

`.claude/commands/log.md`에 정의된 `/log`를 호출하면 오늘 날짜 파일에 세션의
작업/AI 활용 내역을 요약해서 추가한다. 이때도 위 절대 규칙을 그대로 적용한다.

## 커밋 컨벤션

Conventional Commits(`feat:`, `fix:`, `chore:` 등) + 설명은 한국어.
