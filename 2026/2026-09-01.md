# 2026-09-01

## 작업

- 홈서버(raspi, Raspberry Pi 4 / Arch Linux ARM)에 연결한 저장장치들을 진단.
  - USB 메모리 3개(8/16/32GB) 내용물 확인: 32GB는 완전히 비어 있어 재사용 가능,
    8GB는 Ubuntu 설치 미디어, 16GB는 Tails + LUKS(불필요 확인).
  - 외장 HDD/SSD를 하나씩 갈아끼우며 SMART + badblocks 전체 읽기(비파괴) 스캔:
    - 1TB 도시바 2.5" HDD: SMART에 재할당/대기 섹터·비정정 오류 다수 →
      사용 보류(폐기 대상).
    - 320GB 삼성 3.5" HDD: badblocks 0, SMART 클린. 노후하지만 정상.
    - 128GB 샌디스크 SSD(Z400s): 정상, 수명 약 91% 잔여.
    - 128GB 샌디스크 SSD(X400): 호스트 쓰기량이 TBW 정격의 10배 이상, 스캔 중
      실시간으로 불량 블록이 증가 → 수명 말기, 데이터 용도 부적합.
    - 2TB 씨게이트 IronWolf HDD: 롱 셀프테스트 "완료(오류 없음)", SMART 클린.
      노후(가동 5년+)라 이중화/백업 역할로 권장.
  - smartmontools 일회성 설치와 스캔을 위해, 실행 범위를 좁힌(smartctl /
    badblocks / 해당 pacman 설치만) NOPASSWD sudoers 드롭인을 두고 systemd
    transient 타이머로 수 시간 뒤 자동 삭제되도록 셋업. 조기 삭제용 cleanup
    스크립트도 배치.
- 8TB 씨게이트 IronWolf HDD 2개를 btrfs 내장 RAID1로 묶은 볼륨 점검(연결 직후).
  - SMART 정적 검사: 양쪽 overall-health PASSED, 재할당/대기/비정정 섹터·전송
    CRC 오류 전부 0, 에러 로그 없음. 가동 약 4.6년의 매칭 페어라 동시 고장 대비
    RAID1과 별개의 독립 백업 유지 권고.
  - btrfs 레벨: 두 멤버 슈퍼블록 세대 일치(한쪽 뒤처짐/degraded 없음),
    Data·Metadata·System 모두 RAID1 프로파일에 혼합 프로파일 없음, 디바이스별
    누적 I/O·손상 카운터 전부 0. `btrfs check --readonly`(lowmem) "no error found".
  - 전체 표면·데이터 검증(SMART 롱 셀프테스트, btrfs scrub)은 착수 후 중단:
    두 8TB가 오래된 Prolific USB2 브리지 1개(UAS 없음, 480Mbps 공유)에 물려
    scrub 속도 ~14MiB/s·완주 ETA 3.5일이고, 셋업 중 USB 링크가 반복적으로
    끊겼음. 미사용 USB3 포트로 옮긴 뒤 재실행하기로 하고 연기.
  - 부수 확인: 부하 시 디스크 온도 59~62°C(수명 최고 64~65°C)로 높아 엔클로저
    냉각 개선 필요, btrfs space_cache가 deprecated된 v1이라 v2 전환 권장.
  - 앞서 만들어 둔 좁은 범위 NOPASSWD sudoers 드롭인(smartctl/badblocks)을 그대로
    재사용. btrfs/mount는 권한 밖이라 검사 스크립트를 작성해 사용자가 별도 tty에서
    실행하고 진행 로그를 세션이 모니터링하는 방식으로 진행.
- zram(압축 RAM 스왑) 도입.
  - 배경: 디스크/USB를 스왑으로 쓰는 안을 검토했으나 플래시 수명·USB 브리지
    리셋·스왑 무결성 문제로 기각하고 zram으로 결정.
  - server-automation(Ansible) 저장소에 roles/zram 신규 작성:
    systemd-zram-generator 설정과 vm sysctl 드롭인(swappiness, page-cluster)을
    배포하고, daemon-reload -> 셋업 유닛 재기동 핸들러로 즉시 반영. 인벤토리
    그룹 + 플레이북 role 라인 + 노드별 host_vars 튜닝(raspi: 크기=RAM 전체·
    swappiness 180·lzo-rle / 메인 서버: RAM/4·상한 8GiB·swappiness 60)까지 포함.
  - raspi에 우선 적용. 저장소를 다른 작업(vault 갱신)에 쓰고 있어 그 변경을
    먼저 커밋·푸시로 정리한 뒤 진행. 전체 플레이북을 raspi에 돌리면 같은 노드의
    다른 role이 런타임에 학습한 설정을 덮어쓰는 문제가 있어, zram role만 실행하는
    저장소 밖 일회용 playbook으로 격리 적용.
  - 배포 중 버그 2건 수정: (1) 핸들러를 block으로 감싸 notify 대상 이름이
    등록되지 않던 문제 -> 평평한 핸들러로 분리, (2) 이 커널의 zram이 lzo/lzo-rle만
    지원해 zstd 지정이 조용히 무시되던 문제 -> 압축 알고리즘을 조건부 출력하고
    raspi는 lzo-rle 명시. 저장소 밖 playbook이라 host_vars가 안 잡혀 기본값이
    배포된 것도 함께 수정.
  - 결과: raspi에 /dev/zram0(크기 = 물리 RAM 전체, lzo-rle, 우선순위 100,
    vm.swappiness 180) 활성화, 재부팅에도 유지되는 구성. 메인 서버 몫은
    role/그룹/예시 host_vars에 준비만 해 둠(현재 Ansible 대상 아님).
- 16GB USB 메모리(위 진단에서 Tails+LUKS로 확인됐던 것)를 임시 Rust 개발
  워크스페이스로 재사용.
  - 단일 파티션 + ext4로 포맷(파티션/포맷 자체는 사용자가 수행), 마운트 지점에
    붙이고 소유권을 일반 사용자로 이전. 장치 식별 시 상시 연결된 대용량 btrfs
    디스크와 헷갈리지 않도록 by-id/모델명으로 재확인.
  - raspi에 Rust 툴체인 셋업: rustup으로 stable 설치(PATH는 설치기가 건드리지
    않게 하고, 공용 dotfiles 저장소의 zshrc에 cargo env source 한 줄을 직접
    추가해 커밋·푸시).
  - 개인 Rust 프로젝트(워크스페이스 구성)를 이 USB에 클론하고, 저장소가 고정한
    크로스 컴파일 타깃 동기화 후 `cargo check --workspace` 통과까지 확인.
- Hermes Agent(Discord 할 일 비서) 설정 드리프트를 수정.
  - 배경: 앞선 zram 작업 중 `--check --diff`에서 발견. `~/.hermes`는 Ansible
    템플릿이 배포하는 파일과 Hermes가 런타임에 스스로 쓰는 파일이 같은 경로에
    있어, Discord 홈 채널/온보딩을 설정하면 Hermes가 config·env에 써 넣은
    값(자발적 비-멘션 메시지를 올리는 홈 채널 지정 등)이 다음 플레이북 실행마다
    조용히 지워지고 있었음. 이전 세션이 남긴 조사 메모를 이어받아 진행.
  - server-automation의 roles/hermes_agent 수정: 홈 채널을 Ansible 변수로
    승격해 defaults + host_vars에서 선언적으로 관리하고, 템플릿이 값이 있을
    때만 관련 블록·환경변수를 렌더하도록 조건부 분기 추가. 상류 기본 설정의
    보안/폴백 모델 예시 주석 블록도 템플릿에 그대로 보존.
  - 같은 채널을 free-response 채널로도 지정(그 채널에선 멘션 없이 모든 메시지에
    응답). host_vars에서 홈 채널 변수를 참조해 ID 중복 없이 설정.
  - Jinja 렌더를 로컬에서 두 경우(홈 채널 지정/미지정)로 테스트하고 결과 YAML
    파싱까지 검증한 뒤 커밋, master로 fast-forward. 실제 플레이북 적용은 사용자가
    수행하고 게이트웨이 재시작 후 정상 동작(프로액티브 포스팅 + free-response) 확인.
  - Hermes가 스스로 관리하는 나머지 무해한 드리프트(config 버전·온보딩 상태·
    따옴표 스타일)는 재시작 시 Hermes가 다시 채우므로 손대지 않기로 함.

## AI 활용

- Claude Code(Sonnet 5, 백그라운드 세션)로 진행.
- 디스크 진단: SMART 속성·badblocks 결과 해석, 사용/폐기 판단, 스왑 활용안에
  대한 의견, 임시 sudo 권한을 좁게 부여하고 자동 회수하는 방식을 함께 설계.
- zram: 노드별 적정 크기·swappiness 추천, 기존 저장소 컨벤션에 맞춘 Ansible
  role 초안 작성, dry-run에서 드러난 핸들러/압축기/변수 로딩 문제를 원인까지
  파고들어 수정. 실제 sudo가 필요한 ansible-playbook 실행은 사용자가 수행하고,
  Claude는 저장소 변경·커밋·푸시와 결과 검증을 담당.
- USB 개발환경: 임시 용도에 맞는 파일시스템(ext4) 추천, 장치 오인 방지용
  식별 절차, rustup 설치와 dotfiles PATH 반영 위치 결정, 프로젝트 클론 및
  빌드 환경 검증까지 진행. sudo가 필요한 소유권 변경은 사용자가 수행.
- 8TB btrfs RAID1 점검(이어서): btrfs RAID1 판정 기준(슈퍼블록 세대 일치·RAID1
  프로파일·device stats·scrub와 check의 차이)을 정리하고, dmesg·sysfs 링크
  속도로 USB2 브리지가 병목임을 규명. SMART 셀프테스트 진행률·온도 모니터링,
  scrub 중단 판단, 검사 스크립트 작성은 Claude가 맡고, sudo가 필요한 btrfs
  설치·마운트·scrub 실행은 사용자가 tty에서 수행.
- Hermes 설정 드리프트: 이전 세션의 조사 메모를 이어받아 권장안(홈 채널을
  Ansible 변수로 승격)을 구현. defaults/템플릿/host_vars 수정, Jinja 렌더·YAML
  파싱 로컬 검증, 커밋·fast-forward 병합을 담당. free-response 채널과 홈 채널의
  차이(인바운드 멘션 면제 vs 아웃바운드 프로액티브 목적지)도 함께 정리. sudo가
  필요한 플레이북 적용·게이트웨이 재시작은 사용자가 수행.
