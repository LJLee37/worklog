# 2026-08-26

## 작업

- Arch Linux ARM(ALARM) 라즈베리파이 초기 설치: `settingfiles` 저장소의
  `raspi-arch` 브랜치 설치 스크립트(`install-sdcard.sh` / `install-firstboot.sh`)로
  SD카드 설치를 진행하고, 설치 중 발견된 이슈에 맞춰 스크립트를 보강함.
- `server-automation` Ansible로 이 노드(raspi) 프로비저닝:
  - RTC(하드웨어 시계)가 없는 호스트에서 `hwclock --systohc`를 건너뛰도록
    common 롤 수정
  - `vault.yml`이 평문으로 커밋되는 것을 막는 pre-commit 훅 추가
  - AUR 대신 pnpm으로 Claude Code를 설치하는 `claude_code` 롤 추가
  - `agentic_coding` 그룹(uv/nvm/pnpm 설치 + Claude Code 전역 설치 +
    노드용 `~/.claude/CLAUDE.md` 배포)을 추가하고 이 노드를 그룹에 편입
  - VPN(strongSwan/IKEv2) 자동화를 이 노드 대상 첫 실전 실행에 맞춰
    하드닝하고, iOS 클라이언트 접속을 위해 인증서 전송 조건을 수정
  - Let's Encrypt + Cloudflare DNS-01 갱신에 더해 Cloudflare DDNS를 30분마다
    갱신하도록 추가
  - VPN/Cloudflare 실제 자격증명을 ansible-vault로 암호화해서 group_vars에
    추가 (값 자체는 이 기록에 남기지 않음)
- PR 2건(#1, #2)을 worktree 브랜치에서 작업 후 merge.

## AI 활용

- 도구/모델: Claude Code
- `server-automation` 저장소에서 Ansible 롤/플레이북 작성 및 트러블슈팅
  (VPN 하드닝, RTC 예외처리, `agentic_coding` 그룹 추가 등)에 활용. worktree
  기반 브랜치로 작업한 뒤 PR로 병합.
- `settingfiles`의 `raspi-arch` 설치 스크립트를 실제 설치 중 발견된 문제에
  맞춰 보강하는 데 활용.
