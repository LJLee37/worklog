# 2026-08-28

## 작업

- **raspi에서 자기 자신을 대상으로 Ansible을 실행해도 Claude Code 연결이
  유지되는지 검증.**
  - `server-automation`의 전체 플레이북을 이 노드에서 로컬 커넥션(`-c local`)으로
    돌리고, 1초 간격 아웃바운드 도달성 probe(하트비트)와 실행 전/후 스냅샷으로
    관찰.
  - 결과: 플레이북 실행 중(방화벽 규칙 재배포 + nftables 서비스 기동/재시작 포함)
    아웃바운드 API 연결·기존 SSH 세션·관련 프로세스 모두 무중단. probe 1,083건
    전부 정상 응답.
  - 근거 정리: 방화벽 output 정책이 accept, input이 established 연결을 통과시키고,
    룰셋 재적용이 conntrack을 유지 → 이미 열린 연결은 영향 없음. 이 노드의
    대화형 터미널도 방화벽이 새로 막는 포트를 타지 않음.
- self-run에 필요한 노드별 변수 파일(`host_vars/<노드>.yml`, 원래 gitignore 대상)을
  실제 값으로 작성.
- 실행 중 dotfiles 롤이 `settingfiles` 작업트리의 미커밋 변경 때문에 실패 →
  해당 변경(`.zshrc`/`set.sh`를 새 환경에서 깨지지 않도록 방어 처리: p10k 설정
  마법사 억제, PATH 보정, 키 없을 때 keychain 생략, 스크립트 멱등화)을
  `settingfiles`의 `raspi-arch` 브랜치에 커밋·푸시해서 해소.
- 플레이북 재실행으로 완주(`rc=0`), 방화벽 최종 상태 정상 확인. 이미 발급된
  인증서를 재사용해 재발급은 발생하지 않음.
- 부수 발견: 이 노드의 `nftables.service`가 룰셋은 로드돼 있으나 런타임 상태가
  `inactive`로 표시되는 기존 특이사항(이번 작업이 만든 회귀 아님, 추후 점검 대상).
- 테스트용으로 임시 부여했던 NOPASSWD sudo 규칙과 vault 암호 파일을 작업 후
  각각 원복·삭제.
- **`server-automation`의 노드별 `CLAUDE.md`(Jinja2 템플릿 `roles/agentic_coding/
  templates/CLAUDE.md.j2`)와 `agentic_coding` 롤 수정.**
  - 의미 있는 작업마다 `worklog` 저장소에 `/log`로 업무일지를 남기라는 "작업 기록"
    섹션 추가(public 저장소라 민감정보 금지 규칙 포함).
  - "더 알아보려면" 섹션을 "이 노드와 Ansible"로 개편 — 저장소가 이 노드에도
    clone돼 있고, 로컬 커넥션 self-run으로 자기 자신에게 적용해도 문제없음을
    확인한 내용 반영.
  - `agentic_coding` 롤 pacman 설치 목록에 `github-cli`(gh)와 `ansible` 추가,
    "이미 깔려 있는 도구" 안내에도 반영.
  - 위 변경을 커밋·푸시.
- 다른 백그라운드 세션이 남겨 둔 미커밋 변경(claude 크로스플랫폼 설치 보강,
  `claude_code` 롤 제거 및 `agentic_coding` 그룹 일원화, dotfiles의 zsh/oh-my-zsh/
  powerlevel10k/vim-plug 부트스트랩)을 stash로 잠시 빼두고 작업한 뒤 되살려,
  논리 단위 3개 커밋으로 정리·푸시.
- **(병행 세션) 위 미커밋 변경의 원인 진단·구현·검증.** raspi 프로비저닝 후
  깨져 있던 3가지의 원인을 코드 위치까지 특정:
  - oh-my-zsh·vim-plug 미설치 = `server-automation`에 설치 태스크가 아예 없었음
    (dotfiles 롤이 `.zshrc`/`init.vim` 심볼릭 링크만 걸고, 그 링크가 의존하는
    oh-my-zsh·powerlevel10k·`plug.vim`은 아무도 설치하지 않음 — 원래
    `settingfiles` 부트스트랩 스크립트에만 있던 단계).
  - Claude Code가 잘못된 플랫폼용(`claude.exe` 못 찾음) = 설치 경로가 두 롤에
    중복 + 아키텍처 미지정 + `creates:` 가드로 재실행 스킵 → 이미지 준비 단계에서
    섞인 바이너리가 고착.
  - 수정 = dotfiles 롤에 oh-my-zsh/powerlevel10k clone·`plug.vim`·헤드리스
    `PlugInstall` 태스크 추가, agentic_coding 롤은 `ansible_facts` 기반
    `--os`/`--cpu` 명시 + 실행 확인해 실패 시 제거·재설치하는 자가복구 추가,
    중복 `claude_code` 롤 삭제(위 3개 커밋).
  - 검증 = 이 노드에 `uv`로 `ansible-core` 설치 후 dotfiles+agentic_coding 롤만
    로컬 커넥션으로 적용 → oh-my-zsh·`plug.vim`·nvim 플러그인 10개 설치되고
    `claude`가 arm64로 정상, 자가복구는 정상 설치엔 무동작, 재실행 무변경(멱등).
    pnpm `--os`/`--cpu`가 플랫폼별 optional dependency를 실제로 거르는지 별도
    패키지로 실측 확인.
- **아침 브리핑 자동화 구축** (할 일을 마크다운으로 관리 + 매일 아침 브리핑 발송).
  - 새 프라이빗 GitHub 저장소 `daily-briefing` 생성: 할 일 표(`todo.md`), 생성
    프롬프트(`prompt/morning-briefing.md`), 러너/발송 스크립트(`bin/*.sh`), 시크릿
    차단 pre-commit 훅(`worklog` 훅을 프라이빗용으로 조정).
  - 브리핑 구성: 오늘 할 일 / 지연된 일 / 서울·수원 날씨 / 종합 뉴스 10건 /
    IT 뉴스 5건. 헤드리스 `claude -p` + 웹 검색으로 생성.
  - 뉴스 출처를 기사 직접 링크로, 공신력 매체 화이트리스트(통신사·공영방송·주요
    일간지·정책브리핑, Reuters/AP/BBC, 전자신문/ZDNet Korea 등)로 한정. "종합"류
    모호한 출처·블로그·SNS·애그리게이터 금지. 실제 생성으로 링크 형식 검증.
  - `server-automation`에 `morning_briefing` Ansible 역할 신설(`letsencrypt_cf`
    패턴 준용): `msmtp`·`jq` 설치, 저장소 clone, 비밀값(Discord 웹훅 URL·Gmail 앱
    비밀번호)을 vault에서 `~/.config`로 배포, `morning-briefing.service`/`.timer`
    템플릿·활성화. `inventory/hosts.yml`에 `briefing_hosts` 그룹, `site.yml`에
    역할, `vault.yml.example`에 키 추가.
  - 멀티 호스트 failover: 호스트별 rank(인벤토리)로 타이머 시각을 계산(rank 1 =
    06:30, 이후 rank 1당 +15분). 러너가 발송 전 오늘자 브리핑을 커밋·푸시로
    "선점" → 발송은 하루 한 번, 주 호스트 장애 시 예비가 대행. 현재 main-server
    미설정이라 raspi(rank 2, 06:45) 단독 운영.
  - 비대화형 git 푸시: SSH 우선, 실패 시 HTTPS + `gh` 자격증명 헬퍼로 폴백.
  - raspi에 플레이북 적용 후 Discord·이메일 양쪽 수신 검증 완료.
  - 이후 할 일 파일을 활성(`todo.md`: 예정·진행중)과 완료 아카이브
    (`todo-archive.md`: `완료일` 열 추가)로 분리.
- **`daily-briefing`에 반복 할 일 자동화 추가 + 실제 할 일 등록.**
  - `todo.md`에 잡무/개발/취업/대학원 카테고리의 일회성 할 일 11건 추가하고
    예시 행 제거.
  - 매일/매주 반복되는 루틴은 `recurring.md`(신규)로 분리 — `주기` 열이
    `매일` / `평일`(월~금 중 공휴일 제외) / `매주 <요일>`를 표현. 아침 브리핑이
    매일 이 파일을 읽어 오늘 조건에 맞는 항목만 "오늘 할 일" 맨 위에
    `· 반복(...)` 표시로 자동 삽입. 등록: 운동(매일)·영상 시청 루틴(평일)·주간
    계획표 작성(매주 금).
  - `prompt/morning-briefing.md`에 `recurring.md` 읽기와 주기 해석 규칙 추가,
    `README.md`/`CLAUDE.md`에 반복 파일 규칙 문서화. 러너·systemd·Ansible은
    건드리지 않음(프롬프트 변경만으로 다음 실행부터 반영되는 구조).
  - `feat:` 커밋 후 푸시.
- **opencode(Ollama Cloud 백엔드)를 `agentic_coding` Ansible 역할에 추가.**
  - opencode(`opencode-ai`)를 Claude Code와 동일 패턴으로 pnpm 전역 설치
    (플랫폼 플래그 명시 + 실행 실패 시 제거·재설치 자가복구) 후
    `/usr/local/bin`에 심링크하는 태스크 추가.
  - Ollama Cloud를 opencode 커스텀 provider(openai-compatible 엔드포인트)로
    설정하는 `opencode.json` 템플릿 신설. API 키는 vault
    (`agentic_coding_ollama_api_key`)에서 받아 `~/.config/opencode/`에
    mode 0600으로만 배포하고, 키가 없으면 provider 설정은 건너뛴다. 기본 모델은
    GLM 5.3 Flash(cloud), 노출 모델 목록·기본 모델·패키지 버전은 역할 기본값
    변수로 조정.
  - 전역 지침을 Claude Code와 공유하도록 `opencode.json`의 `instructions`와
    `~/.config/opencode/AGENTS.md`가 `~/.claude/CLAUDE.md`를 참조하게 함.
  - `CLAUDE.md.j2` "이미 깔려 있는 도구"에 opencode 항목, `vault.yml.example`·
    `README.md`에 새 키 반영.
  - 템플릿을 Jinja로 렌더해 유효 JSON·키 이스케이프·기본 모델 생략 동작을
    확인하고 `ansible-playbook --syntax-check` 통과. 실제 키 입력과 플레이북
    적용·동작 검증은 사용자 몫으로 남김.

## AI 활용

- 도구/모델: Claude Code (백그라운드 세션)
- `server-automation` 저장소를 조사해 raspi에 적용되는 롤과 네트워크에 영향을 줄
  수 있는 태스크(sshd/nftables/VPN)를 식별하고, 로컬 self-run + 하트비트 관찰
  계획을 세워 실행·분석까지 진행.
- 하트비트 로거 스크립트 작성, 실행 전/후 시스템 스냅샷 비교, 결과 리포트 작성.
- dotfiles 차단 이슈를 진단하고 `settingfiles` 변경을 커밋·푸시해 해소.
- `server-automation` 저장소 구조를 조사해 CLAUDE.md 템플릿/롤에서 고칠 위치를
  식별하고, 수정과 Conventional Commits 메시지 작성을 진행. 기존 미커밋 변경과의
  충돌은 stash/pop으로 처리하고 커밋을 논리 단위로 분리.
- 도구/모델: Claude Code (Sonnet, plan mode → 구현) — 병행 세션. `settingfiles`와
  `server-automation` 두 저장소를 Explore 에이전트로 조사해 세 증상의 원인을
  코드 위치까지 특정하고 계획 수립 후 구현. pnpm 아키텍처 플래그 동작을 실측
  검증하고, `uv tool`로 ansible-core를 설치해 범위 한정 검증 플레이북을
  작성·실행·멱등성 확인.
- 도구/모델: Claude Code (Sonnet, plan mode → 구현) — 아침 브리핑 자동화.
  요구사항을 질의응답으로 정리해 전달 방식(Discord+이메일)·호스팅(raspi
  systemd)·failover(프라이빗 git 저장소를 조정 지점으로)·Ansible 역할화 방향을
  확정하고 계획 수립.
- `server-automation` 기존 역할(`agentic_coding`/`letsencrypt_cf`/`dotfiles`)을
  조사해 컨벤션(템플릿 헤더, `become` 분리, 타이머 활성화 방식, vault 사용)에
  맞춰 새 역할·템플릿·인벤토리/플레이북 수정 작성.
- 헤드리스 `claude -p` 웹 검색 가용성, SSH·HTTPS 두 푸시 경로, OnCalendar rank
  계산식, 플레이북 syntax-check, Discord 2000자 청크 분할, 브리핑 실제 생성을
  각각 실측 검증. 커밋 메시지는 Conventional Commits + 한국어.
- 비밀값(웹훅 URL·앱 비밀번호)은 세션에서 다루지 않음. vault 입력과 sudo가
  필요한 플레이북 실행·정상 동작 확인은 사용자가 수행.
- 도구/모델: Claude Code (Sonnet) — `daily-briefing` 반복 할 일 자동화.
  저장소 구조를 읽고 반복 개념을 넣을 위치를 검토, 러너·systemd 변경 없이
  `recurring.md` + 프롬프트 수정만으로 구현하는 방안을 제시. 반복 주기·요일은
  질의로 확정(주간 계획표=금, 영상 시청=평일·공휴일 제외, 청소 항목은 반복
  제외). 파일 편집·커밋·푸시·업무일지 작성 수행.
- 도구/모델: Claude Code (Sonnet, plan mode → 구현) — opencode + Ollama Cloud
  역할 추가. Explore 에이전트로 `agentic_coding` 역할 구조와 저장소 비밀값
  관례(ansible-vault → mode 0600 템플릿 배포)를 조사하고, opencode의 Ollama
  Cloud 설정 방식(openai-compatible provider)과 최신 클라우드 모델명을 웹에서
  확인해 계획 수립. 키 전달 방식(vault→0600)과 기본 모델(GLM 5.3 Flash)은
  질의로 확정. Jinja 템플릿을 로컬에서 렌더해 JSON 유효성·이스케이프를 검증하고
  `--syntax-check` 통과 확인. 실제 API 키 입력과 플레이북 적용·동작 검증은
  사용자가 수행.
