# 2026-08-29

## 작업

- 림월드/오디세이 DLC에서 영감을 받은 "배 중심 콜로니 시뮬레이션 게임"
  (`driftline`) 개발 준비. 새 저장소를 만들고 설계 문서 세트 + 크레이트 골대만
  담아 첫 커밋.
  - 확정한 방향: 구현 언어 Rust(C#/Java/Kotlin 제외), 2D 탑다운 실시간(일시정지)
    + 다층(z-level), 타일 단위로 조립하는 이동식 배, 하이브리드 멀티플레이
    (결정론 lockstep 협동 + 조우 인스턴스), 데이터+Lua+WASM 3계층 모딩(아키텍처
    무관 동일 산출물), 발현적 연구(클릭식 트리 없음), fixed-point 결정론 시뮬,
    MIT 라이선스.
  - 5차에 걸쳐 요구사항을 받아 계획에 반영: 시간/낮밤·니즈/건강(괴혈병)·위생·
    특성, 시간 척도 상수화, 물자/의료/식품 보존 카탈로그, 의뢰(계약)·거래·교섭
    협상 표면, 선내 기반시설(온도 필드·냉난방·냉수/온수 분리 배관·담수화·오수
    처리·전력망·연료·장갑 내구도), 월드 생성(크기·시드)·시작 시나리오·오프맵
    육지 시뮬·성능/그래픽 옵션의 결정론 경계.
  - 산출물: 루트 파일(CLAUDE.md + @CLAUDE.md 포함 AGENTS.md, README, LICENSE,
    CONTRIBUTING, .gitignore, .editorconfig, rust-toolchain.toml,
    workspace Cargo.toml), docs/ 11종(DESIGN·SYSTEMS·ITEMS·INFRASTRUCTURE·WORLD·
    ARCHITECTURE·DETERMINISM·MULTIPLAYER·MODDING·ROADMAP·GLOSSARY),
    crates/ 5종 골대(core/client/net/mod/server 최소 스텁), example-mod 자리표시,
    CI 워크플로 골대(6타깃 매트릭스, 현재 비활성).
  - 이 노드에 Rust 툴체인이 없어 `cargo check`는 미실행(문서·커밋에 명시).
  - 개인 GitHub 계정에 프라이빗 저장소로 생성 후 push. 추후 퍼블릭 전환 예정이라
    처음부터 공개 전제로 관리(시크릿 금지).
  - 이후 테스트 방침 문서(`docs/TESTING.md`)를 추가: 결정론 리플레이 하네스를
    중심축으로 하는 4개 축 정리, 리플레이 하네스·CI 활성화를 M1에서 코드보다 먼저
    세운다는 갭 명시. 5개 문서에서 교차 참조.

## AI 활용

- Claude Code(Sonnet 5)로 진행. 플랜 모드에서 요구사항을 5회에 걸쳐 받아
  단계적으로 설계 계획을 구체화하고, 모순·미확정 지점(엔진 언어, 멀티플레이
  구조, 모딩 기반, 연구 방식, 시대 범위, 배 생활 당위성 등)은 객관식으로 물어
  결정. 승인 후 저장소 스캐폴딩과 문서 11종·크레이트 골대를 생성하고 git 초기화·
  프라이빗 원격 생성·push까지 수행.
