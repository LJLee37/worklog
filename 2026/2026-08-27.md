# 2026-08-27

## 작업

- 작업 기록을 남기기 위한 public GitHub 저장소 `worklog` 구성을 결정
  (구조: `<YYYY>/<YYYY-MM-DD>.md`, 하루에 파일 하나).
- 이 라즈베리파이에 전역 git 사용자 정보(`user.name`/`user.email`)가 아예
  없던 것을 발견하고 최초 설정.
- `worklog` 저장소 뼈대 구성:
  - `README.md`(공개 저장소 안내), `CLAUDE.md`(민감정보 절대 커밋 금지 지침 —
    이 저장소를 여는 어떤 AI 세션에도 적용)
  - `githooks/pre-commit`: 흔한 시크릿 패턴(개인키, AWS 키, password/token류
    값 등)이 스테이징되면 커밋을 막는 안전망 훅
  - `.claude/commands/log.md`: 세션 작업/AI 활용 내역을 오늘 날짜 파일에
    요약해 추가하는 `/log` 슬래시 명령
- 어제(2026-08-26) 라즈베리파이 셋업 내용을 `2026/2026-08-26.md`로 소급 기록.

## AI 활용

- 도구/모델: Claude Code
- 공개 작업기록 저장소를 어떻게 구성할지(디렉터리 구조, 민감정보 유출 방지
  방안, AI 세션이 항상 기록을 남기게 하는 방법)를 설계 단계부터 논의하고,
  실제 저장소 뼈대(README/CLAUDE.md/pre-commit 훅/`/log` 커맨드)와 첫 기록을
  함께 작성함.
