# 2026-09-04

*(전날 밤 M1 구현 세션에서 이어짐 — 2026-09-03 기록 참고)*

## 작업

### tick 길이 재결정 — 이번엔 입력 지연 기준으로

- 전날 남겨 둔 "1배속 입력 지연은 검증되지 않은 가정" 항목을 결론으로 옮겼다.
  **1 tick = 인게임 1/16초(62.5 ms).** 명령이 tick 경계에 붙으므로 tick 길이가 곧 "키가
  움직이기 시작하기까지의 최악 지연"인데, 0.5초면 500 ms다. 무거운 배가 아니라 굼뜬
  프로그램으로 읽힌다. 62.5 ms는 차량 조작이 즉각으로 느껴지는 구간 안이다.
  물리 서브스텝은 4 → 2회로 줄였다 — tick이 곱아지면서 필요가 없어졌다.
- **밀리초 헬퍼를 마이크로초로 바꿨다.** 62.5는 정수 밀리초가 아니라서, 그대로 뒀으면
  클라이언트 시계가 **0.8% 느리게** 흘렀을 것이다. 어떤 테스트도 잡지 못하고 긴 세션마다
  드러날 드리프트였다.
- **시스템별 주기(cadence)를 스케줄에 넣었다.** 고운 tick이 콜로니 연산량을 끌어올리지
  않게 하는 장치다 — 조타만 매 tick 돌고 느린 시스템은 주기를 받는다. 주기도 결정론
  계약의 일부라 이름과 함께 노출해 테스트가 고정한다. 시스템이 하나뿐인 지금이 넣기에
  가장 싼 시점이었다.
- 달력 시간을 물리 시간에서 떼어 tick만 곱게 하는 안은 **기각**했다. 그러면 "몇 시간
  항해했으니 몇 km"가 어긋나 시계가 속도계와 거짓말을 하는데, 항해가 곧 속도의 적분인
  이 게임에서는 그 어긋남이 정면으로 보인다. 압축을 달력과 물리에 같이 걸면 그 문제가
  없고, 지연은 tick을 곱게 해서 풀면 된다.
- 덤으로 1/16초와 1/32초는 2의 거듭제곱이라 고정소수점에서 **정확히** 표현된다. 적분기의
  시간 간격에 반올림 잔차가 없다는 뜻이고, 이건 고르고 나서 알게 된 것이다.
- 리플레이 픽스처를 새 tick률로 다시 굽고, 테스트·린터·6타깃 점검을 다시 돌렸다.
  푸시 후 CI 10개 잡 전부 통과.

### 설계 문서 확장

- **견시 절 신설**: 경보가 성립하려면 배가 위험을 알아야 하는데 그 앎이 공짜가 아니라는
  것을 규칙으로 세웠다. 탐지 세 경로(견시·해도·수심 측정), 탐지 거리를 정하는 것들
  (밝기·안개·견시 위치의 높이·폰의 특성과 니즈), 오탐과 놓침은 난수 없이 결정론적으로
  유도. 이미 있던 시스템 넷을 하나로 묶는 고리가 됐다.
- **조타 권한 확장**: 위임과 직접 조타를 평시든 경보 중이든 원할 때 오갈 수 있게 하고,
  경보 방침을 "인계 / 위임 유지" 둘로 뒀다. AI 항해사가 플레이어와 **같은 조작 표면**을
  쓴다는 제약을 명시했다 — 그래야 AI가 사람은 못 하는 회피를 하지 못하고, 인계와 재위임이
  매끄럽고, "실력이 곧 조작"이라는 기둥이 폰에게도 적용된다.
- **줄 끊기**: 돛의 줄은 무엇을 끊느냐에 따라 결과가 다르다는 것을 조사해 표로 넣었다.
  아딧줄을 끊으면 돛은 올라간 채 힘만 빠지고(펄럭이며 스스로 찢어진다), 마룻줄을 끊으면
  돛이 떨어지고, 활대 조종줄은 통제 상실에 가깝고, 버팀줄은 돛대가 부러진다. 전부 대가가
  있는 지름길이라 "인원이 부족한 배는 폭풍에서 더 비싸게 산다"가 계산 결과로 나온다.
  계류삭·예인삭 같은 연결의 줄은 경로 그래프의 간선을 지우는 것으로 따로 다뤘다.
- **구명보트**: "배가 배를 싣는다"가 편의 기능이 아니라 상태 모델 변경이라는 것을 절로
  분리해 명시했다. 적재 시 복원성 기여, 진수 후 독립 강체, 두 배 사이 승무원 이동까지
  걸린다. 쉬운 추가로 잡아 두면 나중에 아플 부류라 미리 적었다.
- 문서 절 번호가 밀리면서 저장소 전체의 교차 참조를 맞췄다.

## AI 활용

- Claude Code(Opus 5)로 진행. 조사와 문서 초안, 코드 변경을 맡기고 무엇을 결정으로
  남길지는 직접 정했다.
- 유용했던 지점: 62.5 ms로 가자고 했을 때 그대로 따르지 않고 **밀리초 헬퍼로는 62.5를
  표현할 수 없어 시계가 0.8% 느려진다는 것**을 먼저 짚은 것. 조용히 지나갔으면 한참 뒤에
  "왜 시간이 안 맞지"로 나타났을 종류다.
- 돛의 줄을 끊는 효과처럼 도메인 지식이 필요한 부분은 조사 결과를 받아 규칙으로 옮기는
  방식으로 진행했고, 무엇을 규칙화하고 무엇을 재앙으로 분류할지는 직접 판단했다.
- 아쉬웠던 지점(전날에서 이어짐): tick 길이 1차 결정에서 입력 지연을 판단 기준에 넣지
  않았던 것. 처음부터 조작 지연을 기준 목록에 넣었어야 했고, 그랬으면 결정을 두 번 하지
  않았다.

---

*(같은 날 오후 — 데스크톱 확인과 그로부터 나온 것들)*

## 작업

- **작업트리 정리**: 어제 병렬로 쓰던 작업트리 3개를 제거했다. 전부 main에 병합돼 있어
  이력은 병합 커밋에 남고, 각각의 빌드 산출물 디렉터리까지 합쳐 **4.6 GB를 회수**했다.
- **실측 TPS 카운터를 넣었다**(요청). 0.5초 창으로 실제 tick/초와 프레임/초를 재서 창
  제목과 화면 막대, 그리고 상태 해시 로그 줄에 함께 띄운다.
  - 이게 없으면 **감지할 방법이 없는 실패**가 하나 있다: 요청한 압축률만큼 tick을 돌리지
    못하면 게임이 버벅이는 게 아니라 **인게임 시간이 조용히 느리게 흐른다.** 화면은
    멀쩡하고 시계만 거짓말을 하는데, 시계로 항해 거리를 가늠하는 게임에서는 치명적이다.
  - 목표치는 "속도 배율 ÷ tick 길이"이고 일시정지는 0으로 둔다(요청한 게 없으니 뒤처진
    것도 아니다).
- **데스크톱에서 실제로 띄워 확인했다.** 개발 노드가 헤드리스라 CI 아티팩트를 받아
  실행하는 경로다. 창이 뜨고, 시간이 흐르고, 배 마커와 침로 표시가 보인다.
  - **뜻밖의 수확**: 서로 다른 빌드에서 서로 다른 속도로 돌린 두 실행의 상태 해시가
    같은 tick에서 **완전히 일치했다.** 속도 배율이 클라이언트 로컬이고 월드 상태에 새지
    않는다는 불변식이 실물로 확인된 것이다 — 리플레이 픽스처가 잡는 성질을 실제 플레이
    경로에서 본 셈이고, 멀티플레이가 성립하려면 반드시 필요한 성질이다.
- **속도가 여전히 느리다는 지적을 받고 계산해 보니 맞았다.** 하루가 인게임 86 400초라
  그때 상한이던 120배는 하루에 12분인데, 비교 대상 게임의 세 속도는 하루가 각각 16.7분·
  5.6분·2.8분이다. **우리 최고 속도가 그쪽 최저 속도보다 조금 빠른 정도**였다.
  - 사다리를 배율이 아니라 **하루가 걸리는 실제 시간**으로 다시 골랐다 —
    24시간 / 4시간 / 24분 / 6분 / 2분. 플레이어가 느끼는 것이 배율이 아니라 그쪽이다.
  - 같이 버그 하나가 잡혔다. 프레임당 tick 상한이 **고정값**이라 빠른 칸을 조용히 제한할
    참이었다. 실제 시간 기준으로 바꿨는데, 처음 0.5초로 잡았더니 이번엔 1배속에서 그게
    32 tick뿐이라 평범한 프레임 지터에도 시간이 버려지는 것을 테스트가 잡아냈다.
    2초로 늘려 해결. 방금 만든 TPS 카운터가 겨냥한 실패 유형의 실물 사례였다.
- **M1을 닫았다.** 마일스톤 문서에 미체크 항목이 하나도 남지 않았다. CI는 네 차례 모두
  전 잡 통과했고, 이 저장소에서 CI가 실제로 돈 것은 이번이 처음이다(그전 커밋은 전부
  건너뛰기였다).
- **다음 마일스톤으로 넘길 것을 기록했다.** 화면 안에 인게임 시계와 현실 시계를 함께
  띄우는 것, 두 시계의 초 표시를 플레이어 옵션으로 두는 것, TPS 카운터를 막대가 아니라
  숫자로 보여 주는 것. 셋 다 원인이 같아서(폰트 렌더러가 없다) 한 절로 묶었다.
  - 제약 하나를 확정으로 못박았다: **현실 시각은 클라이언트 로컬 표기용일 뿐**이고
    월드 상태·상태 해시·명령·세이브 어디에도 들어가지 않는다. 들어가는 순간 리플레이가
    재현되지 않는다. "세이브에 저장 시각도 넣자" 같은 자연스러워 보이는 제안이 나올
    자리라 미리 적었다.
  - 초 표시는 인게임 쪽에 함정이 있다. 인게임 초는 실재하는 단위지만 압축이 걸리면
    읽을 수 없다(최고 속도에서 실제 1초에 인게임 12초가 지난다). 정작 쓸모 있는 자리는
    1배속 조타 — 좁은 수로에서 선회 타이밍을 재는 그 순간이다.

## AI 활용

- Claude Code(Opus 5)로 진행. 코드 변경과 문서화를 맡기고, 무엇을 넣고 무엇을 다음으로
  넘길지는 직접 정했다.
- 유용했던 지점:
  - **속도가 느리다는 체감을 숫자로 확인시킨 것.** "하루가 몇 분에 지나가는가"로 물었더니
    양쪽 게임의 하루 tick 수와 배속을 실제로 계산해 12분 대 2.8분이라는 비교를 냈고,
    그래서 사다리를 배율이 아니라 하루 소요 시간으로 다시 짜게 됐다.
  - **사다리를 고치면서 딸려 있던 상한 버그를 같이 잡은 것.** 그리고 그 수정이 과했던
    것을 테스트가 잡아 되돌린 것.
  - 로그를 붙여 줬더니 두 실행의 해시가 같다는 것을 스스로 대조해 짚어낸 것. 요청한
    확인은 아니었는데 결정론 불변식의 실물 증거가 됐다.
- 아쉬웠던 지점: 속도 사다리를 **두 번 고쳤다.** 처음엔 8배까지, 다음엔 120배까지였는데
  둘 다 "얼마나 빨라야 충분한가"를 기준 없이 정했기 때문이다. 처음부터 "하루가 몇 분에
  지나가야 하는가"로 물었으면 한 번에 끝났다.

---

*(server-automation 저장소 — VPN 라우팅 + USB 프린터 공유)*

## 작업

- 라즈베리파이(raspi)에 USB로 연결한 HP 컬러 레이저젯 프린터를 CUPS+Avahi로 홈 LAN과 VPN
  클라이언트 대역에 네트워크 프린터로 공유하는 Ansible role(`roles/printer`)을 신규 작성해
  배포. 실기기에서 큐 생성 후 아이폰·Windows에서 인쇄 확인.
- strongSwan VPN(IKEv2, 세 노드가 공유하는 role)에 접속한 클라이언트가 서버 자기 자신
  IP에만 닿고 같은 서브넷의 다른 호스트나 인터넷으로는 못 나가던 문제를 조사. 원인은
  서버가 IP 포워딩·NAT·방화벽 forward 규칙을 전혀 갖추지 않았던 것(원래 VPN이 "서버
  자신에게만 접속"하는 용도로만 쓰여 그동안 드러나지 않았던 구조적 문제)이었고, 임시
  규칙으로 즉석 검증한 뒤 Ansible role에 sysctl 드롭인 + nftables forward/NAT 규칙으로
  영구화. 실기기(아이폰·Windows·라즈베리파이 자체)로 재현·검증까지 마쳤다.
- 위 두 변경을 관심사별 커밋 2개로 나눠 커밋·푸시.
- 후속으로, VPN 접속 상태에서 프린터가 mDNS(Bonjour) 자동탐색으로는 안 잡히는 걸 확인 —
  멀티캐스트가 서브넷 경계를 못 넘는 구조적 한계(앞서 고친 유니캐스트 포워딩과는 별개
  문제)로 진단하고, IP 직접 지정으로 우회하는 방법을 안내. 근본 해결(VPN을 route-based
  방식으로 전환)은 배보다 배꼽이 커 보류하기로 함.

## AI 활용

- Claude Code(Sonnet 5)로 진행. 백그라운드 세션이라 sudo 대화형 입력이 안 돼서, 검증용
  쉘 스크립트를 파일로 써 두고 사용자가 직접 실행하는 방식으로 우회했다.
- 유용했던 지점:
  - VPN 문제를 "경로가 안 알려짐"이라는 처음 추측 대신, 실제 라이브 시스템(포워딩
    sysctl·방화벽 라이브 룰셋·라우팅 테이블)을 읽어 진짜 원인(포워딩/NAT/방화벽 부재)을
    짚어낸 것.
  - 방화벽 변경안을 커밋하기 전에 별도 네트워크 네임스페이스에 실제로 로드해 문법·의미
    오류를 사전 검증한 것.
  - Ansible 모듈 하나(인라인 콘텐츠 배포)의 변수 해석 실수를 실제 실행 실패로 잡아내고
    저장소 관례(템플릿 파일 분리)로 바로 고친 것.
- 커밋을 임의로 합치지 않고, 사용자가 요청한 대로 관심사별 파일 단위(hunk)까지 쪼개
  2개 커밋으로 정리했다.
- 마지막 "VPN 중 프린터가 안 잡힌다"는 질문에는 되는 척 얼버무리지 않고 mDNS/멀티캐스트의
  근본 한계를 짚고, 더 큰 변경(VPN 방식 전환)의 스코프를 분명히 선 긋고 판단을 사용자에게
  넘겼다.

---

*(server-automation 저장소 — 전역 CLAUDE.md에 병렬 처리 오케스트레이션 지침 추가)*

## 작업

- 다른 프로젝트 하나에서 시험삼아 "병렬로 처리 가능한 건 병렬로" 한 번 시켜봤더니,
  토큰 비용은 더 들었지만 속도·정확도·효율이 모두 나았다. 한 번의 결과이긴 하지만
  방침으로 굳혀 볼 만하다고 판단해, `server-automation`의 `agentic_coding` role
  템플릿(전역 CLAUDE.md를 렌더링하는 원본)에 "작업 오케스트레이션 (복잡도 판단 ·
  병렬 처리)" 섹션을 신설했다.
- 규칙의 골자: 작업을 받으면 바로 착수하지 말고 **복잡도 판단 → 병렬화 가능성 검토 →
  계획 제시 → 승인 → 실행** 순서를 따른다. 병렬 가능하면 서브태스크 목록·모델·의존
  관계·통합 방식을 제시해 승인받고 병렬 실행하고, 불가하면 이유를 밝히고 단일 에이전트
  순차 계획을 제시해 승인받는다.
- Claude Code의 기본 동작은 "사용자가 명시적으로 요청하지 않으면 서브에이전트를 쓰지
  않는다"인데, CLAUDE.md의 지시는 그 예외로 인정된다는 점을 확인하고 그 사실을 지침
  본문에 명시했다. 이 한 줄이 없으면 문서를 써 놔도 규칙이 실제로 발동하지 않는다.
- 1차 초안에서는 "같은 파일을 동시에 쓰지 않는다"를 병렬화의 필수 조건으로 뒀는데,
  검토 후 그 조건을 걷어냈다. git merge로 합칠 수 있으면 worktree로 격리해 병렬
  편집하고, 순차 실행은 병합이 어려운 경우(파일 전체를 겹쳐 재작성, 자동 생성 파일,
  바이너리, 서로의 결과를 봐야 다음 편집을 정할 수 있는 경우)로만 한정하도록 뒤집었다.
  파일 충돌을 회피 대상이 아니라 관리 대상으로 본 셈이다.
- worktree 항목에는 통합 절차(브랜치 순차 merge → 충돌 해소 → 통합 결과 재검증)와,
  git stash 스택이 worktree 간에 공유된다는 함정(병렬 작업 중 stash 금지, 임시 커밋으로
  대체)을 같이 적었다.
- 서브태스크 모델 지정 규칙도 문서화했다: 기본은 성격에 맞춰 자율 선택(넓은 검색·단순
  요약은 경량 모델, 설계·까다로운 디버깅은 고성능 모델), 사용자가 지정하면 그 지정이
  우선, 자주 쓰는 조합은 에이전트 정의 파일의 frontmatter로 고정, 컨텍스트를 그대로
  물려받는 fork형 서브에이전트는 항상 부모와 같은 모델이라 지정이 무시된다.
- 템플릿 Jinja2 파싱과 플레이북 syntax-check로 검증한 뒤 커밋하고 master에 푸시했다.
  worktree에 체크아웃된 브랜치라 master를 직접 체크아웃할 수 없어 `HEAD:master` 형태로
  밀었다.
- 실제 배포(플레이북 실행)는 sudo가 패스워드를 요구해 비대화식으로 돌릴 수 없어,
  사용자가 직접 실행하는 것으로 분담했다.

## AI 활용

- Claude Code(Opus 5)로 진행. "이 방침을 전역 지침에 넣어라"라는 요구를 받아 문서 초안,
  검증, 커밋·푸시를 맡겼고, 규칙의 강도(파일 충돌을 병렬화 차단 조건으로 볼지)는 직접
  결정해 되돌렸다.
- 유용했던 지점:
  - "CLAUDE.md의 지시가 기본 서브에이전트 금지 동작의 예외로 인정된다"는, 문서를 써도
    실제로 작동할지를 가르는 조건을 짚어 지침 본문에 못 박은 것.
  - 서브에이전트 모델 지정이 가능하냐고 물었을 때 자율 선택/사용자 지정/정의 파일 고정
    세 경로와, fork형은 지정이 무시된다는 예외까지 구분해 답한 것.
  - 배포 방법을 정하기 전에 sudo가 패스워드를 요구한다는 사실을 먼저 확인해, 못 하는
    일을 시도하는 대신 사용자 실행으로 넘긴 것.
- 아쉬웠던 점: 첫 초안이 "같은 파일 동시 편집 금지"라는 보수적인 조건을 기본값으로 잡아
  병렬화 기회를 스스로 좁혔다. worktree를 이미 알고 있으면서도 "불가피할 때의 예외"로만
  적어 둔 게 원인이다.

---

*(같은 날 — 짧은 세션: 보존 지점 규칙)*

## 작업

- **마일스톤·릴리즈를 브랜치로 보존하는 규칙을 정했다.** 그동안 저장소에는 계획을 갈아
  엎으며 남긴 아카이브 브랜치 하나만 있었고, "M1이 끝난 그 시점"으로 되돌아갈 이름이
  없었다. 앞으로는 마일스톤을 끝낼 때마다 `milestone/m<n>` 브랜치와 주석 태그 `m<n>`을,
  릴리즈마다 `release/v<x>.<y>.<z>` 브랜치와 태그 `v<x>.<y>.<z>`를 남긴다.
  - **브랜치와 태그를 둘 다 남기기로 했다.** 태그는 못 움직이니 "그 지점이 어디였나"를
    고정하고, 브랜치는 움직일 수 있으니 "그 지점에서 다시 작업하기"에 쓴다. 용도가 달라서
    둘 중 하나를 고를 일이 아니었다.
  - 핫픽스 규칙도 미리 박았다 — 앞선 릴리즈 브랜치를 움직이지 말고 새 릴리즈 브랜치를
    판다. 보존 브랜치·태그는 force-push나 reset으로 되감지 않는다.
- **이미 끝나 있던 M1을 소급 보존했다.** 기준 커밋을 main 끝이 아니라 마일스톤 완료를
  표시한 커밋으로 잡았다 — 그 뒤 두 커밋은 다음 마일스톤용 메모를 적어 둔 것이라 M1의
  산출물이 아니다. 어느 커밋을 "그 시점"으로 볼 것인가가 규칙의 핵심이라 문서에도 그
  판단 기준(체크리스트를 닫고 코드베이스 문서의 기준 시점을 고친 커밋)을 적었다.
- 규칙을 세 군데에 나눠 적었다: AI 세션용 지침, 사람용 기여 가이드, 로드맵 문서. 읽는
  사람이 달라서 같은 규칙을 각자의 자리에 둬야 실제로 지켜진다.
- **낡은 현황 문단을 고쳤다.** 프로젝트 지침 첫머리가 여전히 "현재 마일스톤은 M1",
  "main에는 아직 게임 코드가 없다"였는데 둘 다 사실이 아니게 된 지 하루가 지나 있었다.
- git 사용자 이름·메일이 이 노드에 설정돼 있지 않다는 것도 이번에 드러났다(그동안은
  매번 명령줄에서 지정하고 있었다). 전역 설정으로 박았다.

## AI 활용

- Claude Code(Opus 5)로 진행. "마일스톤과 릴리즈를 브랜치로 보존하라"는 방침만 주고
  이름 규칙과 기준 커밋을 어떻게 잡을지는 선택지를 받아 골랐다.
- 유용했던 지점:
  - **기준 커밋을 main 끝으로 잡지 않은 것.** 시키지 않았는데 최근 커밋들을 열어 보고
    마지막 두 개가 다음 마일스톤용 메모라는 것을 확인한 뒤 그 앞을 골랐고, 근거를 태그
    메시지에도 남겼다.
  - **태그를 같이 쓰자고 제안한 것.** 브랜치만 말했는데 태그가 이 용도의 관례적 도구라는
    점을 한 줄로 짚고, 대신 하자가 아니라 병행하는 안으로 내놓았다.
  - 규칙을 적으면서 옆에 있던 낡은 문단을 발견하고, 고치지 않고 먼저 물어본 것.
- 아쉬웠던 지점: 특별히 없다. 짧고 범위가 분명한 작업이었다.

## 작업

- **Hermes 폴백 노드(라즈베리파이)가 상태 저장소에 자동 커밋을 전혀 하지 않던 문제를
  진단하고 고쳤다.** 에이전트 본인은 메신저에서 "폴백에는 자동 기록 시스템이 없고 메인
  전용으로 설계됐다"고 답했는데 틀린 설명이었다 — 2분 주기 타이머는 폴백에서도 정상
  실행 중이었다.
- 원인은 세 겹이었다.
  - **상태 저장소가 rebase 충돌로 굳어 있었다.** 주기 작업이 상대 노드 커밋 위에 자기
    커밋을 얹다가 heartbeat/소유자 파일에서 충돌했는데, 스크립트가 실패를 "무시"로
    삼키면서 저장소를 unmerged 상태로 방치했다. 그 뒤로는 pull·commit·push가 전부 죽어
    그 노드의 기록이 영구히 멈췄다.
  - **폴백이 이미 인수해 서비스를 돌리는 중인데 "메인 다운" 판정이 계속되는 상태에서는,
    코드가 heartbeat·소유자 갱신도 push도 없이 곧장 종료하고 있었다.** 인수 시점 커밋
    한 건 이후로 기록이 구조적으로 0건이 되는 경로였고 지금이 정확히 그 상태였다.
  - 두 노드가 서로를 다운으로 보는 split-brain 때문에 그 상태에서 빠져나오지 못했다.
- 고친 내용: ① 매 주기 중단된 rebase 잔재를 되돌리는 자가복구 단계 추가, ② 두 노드가
  매 주기 덮어쓰는 상태 파일 두 개는 노드 로컬 git 설정으로 충돌을 자동 해소, ③ 폴백이
  인수 중일 때도 갱신·push하도록 빠진 경로를 채우고 복귀한 메인의 인계 요청을 지우지
  않게 함, ④ 실패 시 git 원문 오류를 로그에 남기게 함.
- 스크래치 저장소로 충돌을 재현해 검증했다. 흔히 쓰는 해소 방식이 그대로는 동작하지
  않는다는 걸 여기서 잡았다 — 내장 병합 드라이버가 아니라 저장소 설정에 정의가 있어야
  해서, 그 정의도 함께 배포하도록 고쳤다.
- 라이브 저장소는 먼저 살려 뒀다. 굳은 rebase를 풀고 충돌 해소 설정을 넣었으며, 되돌리기
  과정에서 날아간 미커밋 메모 한 줄은 미리 떠 둔 백업에서 복구했다. 이후 주기 실행에서
  동기화 실패 로그가 사라진 것을 확인했다. 수정된 스크립트 배포는 아직 남았다.
- 메인 노드도 같은 시각 이후 기록이 끊겨 있어 같은 수정이 필요하다는 것을 확인했다.

## AI 활용

- Claude Code(Opus 5)로 진행. 메신저 대화 로그만 던져 주고 "왜 자동 커밋이 없는지
  파악해서 고쳐라"라고 시켰다.
- 유용했던 지점:
  - 저장소 상태·시스템 로그·reflog를 시간순으로 맞춰 "언제부터 무엇이 멈췄는지"를
    특정한 것. 증상(커밋 없음)과 실제 원인(rebase 교착)이 달랐고, 표면 증상만 보고
    타이머나 권한을 의심했으면 못 찾았을 문제였다.
  - 제안한 해결책이 그대로는 안 먹는다는 걸 스크래치 저장소 실험으로 스스로 잡아낸 것.
    검증 없이 넘어갔으면 배포 후에도 조용히 안 고쳐졌을 문제였다.
  - 라이브 저장소를 건드리기 전에 백업을 뜬 것. 실제로 되돌리기가 미커밋 내용을 지웠고
    백업에서 복구했다.
- 아쉬웠던 지점: 중간에 원인을 실행 환경 문제로 잘못 의심해 한 번 돌아갔다.

---

*(같은 날 — 새 저장소: 이력서·자기소개서 관리)*

## 작업

- **이력서와 자기소개서를 관리할 개인 비공개 저장소를 새로 만들었다.** 지원할 때마다
  이력서를 처음부터 다시 쓰게 되는 이유가 "경력과 프로젝트의 사실이 어디에도 정리돼
  있지 않아 매번 기억에서 끌어내야 하기 때문"이라고 보고, 저장소를 두 층으로 나눴다.
  - `master/` — 분량 제한 없이 **사실만** 쌓는 창고(프로필·경력·프로젝트·기술·학력).
    남에게 보여 줄 문서가 아니다.
  - `resume/`, `cover-letters/` — 지원처를 향해 그 사실을 골라 배치한 **문서**.
  - 규칙 하나를 못박았다: 문서를 쓰다 `master/`에 없는 내용이 필요해지면 **master/에
    먼저 적고** 가져다 쓴다. 이력서에 적은 문장은 전부 면접에서 검증받으므로, 이 규칙이
    "면접 답변이 이력서와 어긋나는" 사고를 막는다.
- **PDF 빌드 파이프라인**: pandoc으로 Markdown → HTML, weasyprint가 인쇄용 CSS를 적용해
  A4로 조판. `make pdf` / `make resume` / `make letters` / `make check`.
  - `make check`가 도구 두 개와 **한글 폰트**의 존재를 확인하고, 없으면 설치 명령을
    알려 준다. 폰트가 빠지면 빌드는 성공하는데 PDF의 한글만 전부 네모로 나오기 때문에,
    조용히 통과하는 것보다 실패해 주는 편이 낫다.
  - 조판 CSS에 넣었던 `word-break: keep-all`(한국어 줄바꿈용)이 weasyprint에서 미구현이라
    경고가 났다. 지우고 그 사실을 주석으로 남겼다.
  - 빌드된 PDF의 압축 스트림을 풀어 임베드된 폰트 이름을 확인해, 한글 글리프가 실제로
    들어갔는지까지 검증했다.
- 부속 문서로 작성 가이드(사실 → 문서 원칙, 성과를 한 줄로 쓰는 법, 자기소개서 문항이
  실제로 묻는 것, 제출 전 체크리스트)와 문항 은행(문항 유형 ↔ 소재 위치 매핑)을 같이 뒀다.
- **제출본 보관 규칙**: 빌드 산출물 디렉터리는 git이 무시하므로, 실제로 낸 파일은 별도
  디렉터리에 복사해 커밋한다. 나중에 "어디에 정확히 무엇을 보냈는지" 확인할 수 있어야 한다.
- 저장소 전용 `CLAUDE.md`에 **"사실을 지어내지 않는다"**를 절대 규칙으로 박았다. 근거가
  없는 경력·수치를 추측으로 채우지 말고 자리만 잡을 것, 표현을 다듬되 주장의 강도는
  높이지 말 것. 덧붙여 worklog가 공개 저장소라는 점을 근거로, 실명·연락처·경력 상세·
  지원처 이름을 그쪽에 남기지 말라는 조항도 같이 넣었다.
- 비공개 저장소를 만들어 최초 커밋을 푸시했다.

## AI 활용

- Claude Code(Opus 5)로 진행. 구조와 문서 초안을 맡기고, 형식(Markdown+PDF)·언어(한/영)·
  지원처별 관리 여부·브랜치 이름은 선택지를 받아 직접 골랐다.
- 유용했던 지점:
  - 착수 전에 **한글 폰트가 설치돼 있지 않다는 것**을 먼저 확인해 알려 준 것. 안 그랬으면
    첫 PDF에서 한글이 전부 네모로 나오는 걸 보고서야 알았을 문제다.
  - 만들고 끝내지 않고 **실제로 빌드를 돌려** CSS 경고를 잡고, PDF 안에 한글 폰트가
    임베드됐는지까지 확인한 것.
  - 경력 내용을 아는 척 채워 넣지 않고 전부 자리표시자로 둔 것.
- 아쉬웠던 지점: 조판 CSS에 렌더러가 지원하지 않는 속성을 먼저 넣었다. 빌드를 돌려 바로
  잡긴 했지만, 쓰기 전에 지원 여부를 확인했으면 될 일이었다.

---

*(같은 날 — 새 개인 프로젝트: 기획·설계 문서 세우기)*

## 작업

- **새 개인 프로젝트의 기획·설계 문서를 처음부터 세우고 비공개 저장소를 만들었다.**
  공적 제도를 일반인에게 설명하는 정보 웹서비스로, 목표를 3단계로 나눴다. 코드는 아직
  한 줄도 없고 문서만 있다.
- 착수 전에 갈리는 결정 네 가지를 먼저 물어 확정했다: 프론트엔드 프레임워크, DB,
  1차 범위, 배포 대상(미정으로 두고 문서에 선택지만 남김). 백엔드는 처음부터 주어진
  제약이었다.
- **문서 10종을 썼다**(약 3,900줄): 비전·로드맵, 1차 세부 계획(마일스톤 M0~M5),
  아키텍처, 콘텐츠 계획, 데이터 모델, 계산 도구 사양, 법무·컴플라이언스, 배포,
  접근성, ADR. 저장소가 나중에 공개로 바뀔 수 있다고 가정하고 코드와 콘텐츠
  라이선스를 분리했다.
- **조사 결과가 계획을 몇 군데 바꿨다.** 이게 문서를 쓴 것보다 큰 소득이다.
  - 만들려던 계산 도구 하나가 **원리상 정확한 결과를 낼 수 없다**는 것이 조사에서
    드러났다. 판정 알고리즘의 마지막 단계가 공개돼 있지 않아 재현이 불가능하다.
    기능을 "예측"이 아니라 "참고용"으로 다시 정의하고, 내부 점수를 산출하지도 표시하지도
    않기로 했다. 결과가 낮게 나와도 사용자의 행동을 만류하지 않는 것을 원칙으로 넣었다 —
    과대 기대만큼 과소 기대도 실제 피해를 낸다.
  - **규제 조사가 뒤 단계의 취급 범위를 좁혔다.** 겉보기에 같은 묶음으로 보이던 대상들이
    적용 법령이 달라서, 한쪽은 초기 범위에서 제외하는 쪽으로 정리했다.
  - 사용자 위치를 자동으로 받는 대신 **직접 선택하게 하는 방식**으로 못박았다. 자동
    수집은 별도 규제 부담이 생길 수 있다. 뒤 단계 기능이지만 지금 설계에 반영해야
    나중에 뒤엎지 않는다.
- **외부에서 확인해야 하는 수치는 1차 출처에서 확인하고 확인 시각을 문서에 박게 했다.**
  매년 개정되는 값이라 "언제 확인한 값인가"가 없으면 문서가 조용히 낡는다. 확인하지 못한
  항목은 지어내지 않고 TODO로 남겼다(50건). 그중 일부는 공개 자료로는 애초에 판정할 수
  없는 값이라, 도구가 자동 판정하지 않고 시나리오별 범위로 보여 주도록 사양을 바꿨다.
- 콘텐츠를 DB가 아니라 저장소 안 파일로 두기로 했다. 출처와 최종 확인일이 없는 글은
  **빌드를 실패시키는** 장치를 넣었다 — 문서가 20개를 넘으면 사람의 성실함으로는 안 새는
  곳이 없다.
- **아직 검토하지 않았다.** 오늘은 생성까지만 하고, 내일 문서 전체를 직접 읽어 볼 예정이다.

## AI 활용

- Claude Code(Opus 5)로 진행. 전역 지침에 넣어 둔 병렬 오케스트레이션 규칙이 처음으로
  실전에서 발동했다 — 복잡도를 판단하고 계획을 제시받아 승인한 뒤 병렬로 돌렸다.
- **서브에이전트 4개를 병렬로 썼다.** 정확도가 중요한 하나만 상위 모델을 지정하고 나머지는
  중간 모델로 뒀다. 기반 문서 둘은 메인이 직접 쓰고, 통합·검증·커밋도 메인이 했다. 서로
  다른 파일만 건드려서 worktree 격리는 쓰지 않았다.
- 유용했던 지점:
  - **"이 기능은 원래 만들 수 없다"는 결론을 스스로 내고 근거를 댄 것.** 시킨 건 사양을
    쓰라는 것이었는데, 조사 끝에 재현이 불가능하다는 걸 확인하고 기능의 성격 자체를 바꿔
    왔다. 모르는 걸 그럴듯하게 채웠으면 사용자가 틀린 결과를 믿는 서비스가 됐다.
  - **묻지 않은 규제 차이를 짚어 로드맵을 바꾼 것.**
  - 수치를 기억으로 채우지 말라고 지시했더니 실제로 지켰고, 확인 못 한 것들을 TODO로
    남겼다. 메인 쪽에서 핵심 수치 두 개를 따로 재검색해 대조했는데 일치했다.
- 아쉬웠던 지점: 특별히 없다. 다만 문서 분량이 3,900줄이라 **검토 부담이 만든 것만큼 크다.**
  병렬로 시키면 만드는 속도는 빨라지는데 읽는 속도는 그대로라, 검토가 병목이 된다는 걸
  이번에 체감했다. 내일 읽어 보고 판단할 일이다.

## 작업

### driftline M2(조타 프로토타입) 구현

M1이 끝난 저장소에서 다음 마일스톤을 착수했다. 목표는 "폐허 사이 수로가 있는 바다에서
배 한 척을 손으로 몰아 암초를 피해 지나가는 상태"이고, **합격 판정이 코드가 아니라
손맛**이라 계획 단계에서 범위를 관문 기준으로 두 겹으로 갈랐다.

- **조종 지형** — 수몰 도시의 가로망이 그대로 수로가 되게 만들었다. 좁은 수로가 노이즈의
  우연이 아니라 도시의 구조로 생겨야 조타 실력이 성립한다. 지형은 시드의 순수 함수라
  저장하지 않고, 캐시는 상태가 아니므로 "비워도 결과와 상태 지문이 같다"를 테스트가 강제한다.
- **선체와 부력** — 외곽선(충돌 판정에 쓰는 그 폴리라인)과 부력체 모듈에서 흘수를 유도한다.
  적재하면 깊이 잠기고 느려지고 더 쉽게 걸린다는 고리가 별도 규칙 없이 계산으로 나온다.
- **3자유도 강체** — 돛(겉보기 바람 + 계수 곡선)·키·저항·요 감쇠·원심항. 물리 상수는
  전부 한 구조체에 모았다. 관문에서 손으로 만질 것들이라 소스 전역에 흩어지면 조작감을
  못 맞춘다.
- **좌초** — 붙잡는 힘이 무는 깊이에 비례해 등급이 있다. 여울을 스치면 속력만 잃고
  지나가고, 무너진 건물에 올라타면 못 벗어난다. 규칙 하나로 "스친 것"과 "항해가 끝난 것"의
  차이를 지형이 만든다.
- **화면** — 배를 그림 한 장으로 그리지 않고 충돌 판정에 쓰는 외곽선을 그대로 그린다.
  나중에 배를 개조하면 화면도 같이 달라져야 인과가 보이기 때문이다. 얕은 곳만 칠하고
  깊은 물은 배경으로 뒀다. 조종 패널(침로·속력·타각·돛·풍각·용골 밑 여유)과 조타 키.
- 리플레이 픽스처 4개(조타 시퀀스 · 좁은 수로 통과 · 좌초 · 하루 정지). 효과 검증은
  "지문이 맞다"에 그치지 않는다 — 수로 양옆에 실제로 폐허가 있었는지, 좌초 지점에서
  15 m 옆이면 멀쩡한지까지 단언한다.

검증: 포매터·린터·부동소수 금지 검사·테스트 226개·6타깃 크로스컴파일 통과, 리플레이가
디버그와 릴리즈에서 같은 지문. **관문(사람이 직접 몰아 보는 판정)은 아직 남았다** —
개발 노드에 디스플레이가 없어 CI 아티팩트를 받아 데스크톱에서 해야 한다.

### 알게 된 것

- **물리에 구멍이 있었다.** 어떤 침로도 유지되지 않고 배가 전부 바람 쪽으로 올라와 서길래
  파고들었더니, 선체 후미가 지느러미로서 만드는 복원 모멘트가 통째로 빠져 있었다.
  표준 조종성 모델에 있는 항이고, 없으면 옆으로 밀리는 것이 회전을 낳고 회전이 다시
  옆으로 미는 되먹임이 끊기지 않는다. 계수 튜닝으로는 절대 안 고쳐지는 종류였다.
- **병렬로 만든 두 갈래가 각자 합리적인 숫자를 골랐는데 서로 어긋나 있었다.** 한쪽은
  "이 깊이면 어떤 배든 못 지나간다"고 적었고 다른 쪽은 그보다 얕게 뜨는 배를 만들었다.
  전제가 조용히 깨져 있었고, 통합 테스트가 "폐허를 못 찾겠다"고 실패하면서 드러났다.
  고치면서 오히려 더 나은 설계가 나왔다 — 적재 여부가 갈리는 지점을 다른 지형 요소로
  옮기니 "지름길이 있는데 가볍게 가야만 지나간다"가 규칙이 아니라 결과로 생겼다.
- **성능 병목이 예상과 달랐다.** tick 비용이 300배로 뛰어서 가장 비싸 보이는 함수를
  의심했는데 실제 기여는 13%였고, 90% 이상은 "물 위에 아무것도 없는데도 매 tick 선체
  둘레 점을 만들던 것"이었다. 값싼 사전 판정을 앞에 두어 10배 넘게 줄였다. **상태 지문이
  그대로였던 것이 이 최적화가 결과를 안 바꿨다는 증거**가 됐다.
- 그래서 이전 마일스톤에 적어 둔 "배가 많아도 여유"라는 문장이 더 이상 사실이 아니게
  됐다. 새 숫자와 다음에 손댈 곳을 표로 갱신해 뒀다. 낡은 자랑을 남겨 두면 나중에 그걸
  믿고 판단하게 된다.

## AI 활용

- Claude Code(Opus 5)로 진행. 전날에 이어 병렬 오케스트레이션 규칙대로, 계획서를 먼저
  쓰고 승인받은 뒤 **서브에이전트 3개를 worktree 격리로 병렬 실행**했다. 서로 다른 파일을
  맡겼는데도 한 파일(모듈 목록)에서 충돌이 났고, 병합·충돌 해소·통합 검증은 메인이 했다.
- **핵심 물리는 메인이 직접 썼다.** 설계 판단이 가장 많고 관문의 핵심이라 나눌 이유가
  없었다. 판단이 얽힌 부분과 병렬로 흩어도 되는 부분을 가르는 게 계획의 실제 내용이었다.
- 유용했던 지점:
  - **테스트가 설계 결함을 잡았다.** "가까이 붙어 항해하는 경우가 빔리치와 같은 속도로
    수렴한다"는 실패 하나가 돛 균형의 부호가 뒤집혀 있었다는 걸 드러냈고, 나중엔 통합
    테스트가 지형과 선체의 가정 불일치를 잡았다. 지어낸 값으로 넘어갔으면 관문에서
    "그냥 재미없다"로만 나타났을 것들이다.
  - **측정 도구를 남긴 것.** 폴라 곡선·선회 반경·타력을 찍는 계기를 테스트로 만들어
    두어서, 상수를 만질 때마다 감이 아니라 표를 보고 판단했다.
  - 성능 병목을 추측으로 고치지 않고 의심 후보를 하나씩 제거해 가며 좁힌 것.
- 아쉬웠던 지점: **병렬 갈래끼리 공유해야 할 전제를 내가(=계획 단계에서) 명시하지
  않았다.** 서로 다른 파일만 건드리게 했는데도 숫자의 의미가 어긋났다. 파일 경계는
  나눠도 **설계 전제는 나눠지지 않는다** — 다음엔 갈래마다 "네가 의존하는 다른 갈래의
  값"을 프롬프트에 박아 두는 편이 낫겠다.
