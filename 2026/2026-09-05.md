# 2026-09-05

## 작업

- **RimWorld 클론 게임을 Fable 5.1 성능 평가 목적으로 처음부터 구현하라고 시켰다.**
  결과물은 `RimWorldClone` 프라이빗 저장소에 커밋·푸시했다 (TypeScript + Vite, Canvas 2D +
  DOM UI, 프레임워크 없음, 약 5.2만 줄).
- 구현 범위: RimWorld 위키를 직접 조사해 옮긴 데이터 주도 def 시스템(사물 300+, 레시피
  100+, 연구 67, 특성 65, 생각 130+, 질병 54, 동물 65, 바이옴 7, 사건 38), 맵 생성, 방
  온도 평형, 전력망, 화재, 날씨, 신체부위 기반 건강(출혈·감염·면역·수술), 잡 시스템(A*
  경로·예약·워크기버), 사회/연애/정신 붕괴, 동물 조련·번식, 포로, 무역, 스토리텔러(위협
  점수·적응도)와 38개 사건, 습격/방문 집단 AI, 전투(엄폐·방어구·포탑·함정), HUD/건축/검사창/
  작업표/일정/연구 트리/세력/역사 UI, 세이브·로드.
- 검증: vitest 22개(정합성 검사 + 헤드리스 시뮬레이션: 운반/건설, 전체 사건 발생, 습격,
  세이브 라운드트립·크기), typecheck/lint/build 통과. Playwright 헤드리스 Chromium으로 새
  콜로니 시작 → 진행 → 모든 탭 → 저장/불러오기를 스크린샷으로 확인.
- **실제 플레이를 해봤더니** 폰들의 현 위치가 정상적으로 표시되지 않는 버그가 있었고,
  브라우저 때문인지 서버 때문인지는 알 수 없으나 스크롤을 시도하면 너무 많이 확대되거나
  너무 많이 축소되는 문제가 있었다. 아직 고치지 않았다.
- 헤드리스 Chromium이 시스템 라이브러리 부족으로 안 떠서, sudo 없이 Steam 런타임의 .so만
  심볼릭 링크해 `LD_LIBRARY_PATH`로 올리는 우회법을 찾아 메모리에 기록했다.

- **daily-briefing 아침 브리핑이 이미 완료한 항목을 계속 내보내고, 새로 추가한
  항목은 빼먹던 문제를 원인 분석하고 고쳤다.** 원인이 네 가지였다.
  - `todo.md`·`todo-archive.md` 거의 모든 줄 앞에 `13|13|` 같은 줄번호 접두사가
    붙어 있었다(9/1 이후, 편집할 때마다 한 겹씩 누적돼 아카이브는 6겹까지 갔다).
    접두사가 붙은 줄은 마크다운 표 행이 아니라서, 9/4 브리핑 시점엔 42줄 중
    39줄이 오염돼 정상 표 행이 3줄뿐이었다. 그래서 브리핑이 항목을 중복
    출력하거나 통째로 놓쳤다.
  - 표 헤더에 열 두 개를 추가했는데 기존 행은 옛 열 수 그대로였고, 러너 awk와
    브리핑 프롬프트도 옛 기준이라 어느 칸이 `상태`인지 확정할 수 없었다. 반복
    행에 완료를 적으면 러너가 태그를 못 찾고 그 행을 버린 뒤 새 행을 만들어
    완료 기록이 증발했다.
  - `유지` 반복 백로그는 완료 표시를 해도 목록에서 사라지지 않는 설계였다.
  - 미래 날짜로 돌린 테스트 산출물(더미 스텁 브리핑)이 커밋돼 있어, 그날
    아침엔 러너가 "이미 발행됨"으로 종료하고 브리핑을 아예 안 보낼 상태였다.
- 수정: 표 전 행을 헤더와 같은 열 수로 정규화, 뭉개진 아카이브를 커밋 이력에서
  복원, 러너를 고정 인덱스 대신 헤더 이름 기반 열 매핑으로 교체(완료 판정은
  원본 줄에서 하므로 열이 밀려도 인식), 완료한 반복 행 자동 제거, 프롬프트를
  새 스키마로 갱신하고 깨진 줄을 브리핑에 보고하는 절 추가, pre-commit에
  줄번호 접두사·열 수 불일치 차단 추가, 테스트용 날짜 override는 dry run으로
  전환해 진짜 브리핑을 선점하지 않게 했다.
- 부수적으로, pre-commit의 "긴 base64 블롭" 검사가 평범한 링크를 키로 오탐해
  할 일 편집을 막고 있었다. URL을 빼고 검사하도록 고쳤고, 꺼져 있던 훅도 켰다.
- 검증: 실제 데이터로 완료 행 제거·같은 날 재실행 멱등성·창 안에서 완료 보존·
  열 불일치 경고를 확인했고, shellcheck와 pre-commit도 통과시켰다.

## AI 활용

- Claude Code(Fable 5.1, effort xhigh)에 "중간에 묻지 말고 처음부터 끝까지" 지시 하나로
  전 과정을 맡겼다. 위키 리서치 → 설계 문서 → 콘텐츠 데이터(서브에이전트 4개 병렬) →
  엔진 → UI → 테스트 → 브라우저 검증 순으로 진행했다.
- 관찰: 콘텍스트가 두 번 압축될 만큼 긴 작업인데도 설계 문서와 타입 계약을 먼저 고정해
  병렬 작성한 파일들이 첫 통합 타입체크에서 26개 오류로 그쳤다. 헤드리스 시뮬레이션
  테스트를 스스로 만들어 "습격 시 틱당 115ms" 같은 눈에 안 보이는 성능 버그(한 틱 안에
  수백 번 돌던 추격 루프), 전부 암석이던 지도 가장자리, 6MB를 넘던 세이브를 잡았다.
- 한계: 헤드리스 스크린샷 검증은 통과했지만 실제 플레이에서는 위의 폰 위치 표시·휠 줌
  버그가 바로 드러났다. 자동 검증이 사람이 조작하는 감각까지는 잡지 못한다.
- Claude Code(Opus 5)에 "원인을 찾아 보고하고, 해결 가능하면 바로 해결하라"고만
  지시했다. git 이력을 커밋별로 훑어 오염이 시작된 커밋, 커밋마다 늘어난 접두사
  겹수, 브리핑이 실제로 항목을 중복·누락한 날을 숫자로 짚어냈다.
- 관찰: "완료 표시가 안 먹힌다"는 증상 하나에 원인이 네 개였고, 그중 셋은 커밋
  이력을 안 봤으면 못 찾았을 것이다. 특히 미래 날짜 스텁은 아직 증상이 나타나지도
  않은(다음 월요일에 터질) 문제라 증상만 쫓았으면 놓쳤을 항목이다.
